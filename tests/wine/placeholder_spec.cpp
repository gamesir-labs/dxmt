#include <dxmt_test.hpp>

TEST(PlaceholderTest, ProvidesCopyableWineStartingPoint) {
  // Copy this file into a subsystem suite and replace this assertion with
  // behavior specific to the code under test.
  SUCCEED();
}
