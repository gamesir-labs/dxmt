# GitHub Actions helpers

| Path | Role |
|------|------|
| `ci-feishu.ps1` | Push / PR / issue cards (Windows self-hosted) |
| `ci-feishu.sh` | Nightly / Component result cards (macOS self-hosted) |

Product tests: `scripts/dxmt-builder test ...` only.
