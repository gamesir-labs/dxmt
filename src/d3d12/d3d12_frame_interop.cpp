#include "d3d12_frame_interop.hpp"

namespace dxmt::d3d12 {

// External-frame interop is intentionally not exposed for baseline D3D12
// rendering. NGX/NVAPI probes must fall back before any external frame resource
// is required.

} // namespace dxmt::d3d12
